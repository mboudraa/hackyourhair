<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent">

    <ImageView
        android:id="@+id/blurred_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        tools:src="@drawable/home_2"/>

    <TextView
        android:id="@+id/blurred_text"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="4dp"
        android:textColor="@drawable/semi_blurred_text"
        android:layout_gravity="bottom"
        android:textStyle="bold"
        android:textSize="26sp"
        android:fontFamily="fonts/leaguegothic_condensed_regular.ttf"
        tools:text="L'Oréal Paris"/>

</FrameLayout>
